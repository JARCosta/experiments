SHELL = powershell.exe

run:
	pip uninstall -y telegramBot
	pip install .

remove:
	pip uninstall -y telegramBot

clean:
	rm -R build
	rm -R *.egg-info

clean-caution:
	rm -R */telegramBot/build
	rm -R */telegramBot/*.egg-info
